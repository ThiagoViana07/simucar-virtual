const cartColor = '#000'
const cartType = 1

export const moveCart = {
  moveRight: (pixels) => {

    return pixels;
  },
  moveLeft: (pixels) => {

    return -pixels;

  },
  moveTop: (pixels) => {

    return -pixels;

  },
  moveBottom: (pixels) => {

    return pixels;

  }

}

